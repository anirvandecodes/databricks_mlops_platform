# This GitHub workflow deploys Bundle resources (ML resource config and more)
# defined under databricks_mlops_platform/resources/*
# and databricks_mlops_platform/databricks.yml with prod deployment target configs,
# when PRs are merged into the release branch
name: Bundle Deployment for databricks_mlops_platform Prod

on:
  push:
    branches:
      - 'release'
  workflow_dispatch:

defaults:
  run:
    working-directory: ./databricks_mlops_platform

env:
  DATABRICKS_TOKEN: ${{ secrets.PROD_WORKSPACE_TOKEN }}

jobs:
  prod:
    concurrency: databricks_mlops_platform-prod-bundle-job
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: databricks/setup-cli@v0.236.0
      - name: Validate Bundle For Prod
        id: validate
        run: |
          databricks bundle validate -t prod
      - name: Deploy Bundle to Prod
        id: deploy
        run: |
          databricks bundle deploy -t prod
